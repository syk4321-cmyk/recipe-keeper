// The uploaded app is intentionally kept in JSX so it remains easy to paste
// and edit as a standalone React component.
// @ts-ignore - Vite handles the JSX module directly.
import RecipeKeeper from './RecipeKeeper.jsx';

function App() {
  return <RecipeKeeper />;
}

export default App;
